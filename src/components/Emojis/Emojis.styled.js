import styled from 'styled-components';

const EmojiWrapper = styled.div`
  height: fit-content;
  width: auto;
  display: flex;
  flex-wrap: wrap;
  p {
    width: 50px;
    height: 50px;
  }
`;

export { EmojiWrapper };
